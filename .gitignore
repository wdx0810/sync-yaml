# Compiled binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
server-linux-*
server-darwin-*
yaml-sync
yaml-sync-*

# Test binaries / coverage
*.test
*.out
coverage.txt

# Node
web/node_modules/
web/dist/

# Embedded frontend build (regenerated by build pipeline)
cmd/server/web/dist/

# IDE / editor
.idea/
.vscode/
*.swp
*.swo

# OS junk
.DS_Store
Thumbs.db

# Local runtime data — must never be committed (contains tokens, kubeconfigs, MFA secrets)
/data/
data/
*.log

# Local config overrides
config.local.yaml
config.local.yml
.env
.env.local

# Patent documents (confidential, keep local only)
专利*
patent_figures/
fill_patent.py
draw_patent_figures.py
rebuild_patent_docx.py
