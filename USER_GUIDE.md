# YAML Sync 使用手册

本手册面向日常使用者（开发、运维、审核人员），讲清楚每个功能页怎么用。系统是 GitLab 与 Kubernetes 之间的双向 YAML 同步平台，所有写操作都带审核。

---

## 目录

1. [登录与账号安全](#1-登录与账号安全)
2. [整体概念](#2-整体概念)
3. [数据源（GitLab / K8s 连接）](#3-数据源)
4. [同步任务](#4-同步任务)
5. [正向同步：GitLab → K8s（带审核）](#5-正向同步gitlab--k8s)
6. [反向同步：K8s → GitLab](#6-反向同步k8s--gitlab)
7. [配置变更（编辑 ConfigMap → 审核 → 提交 GitLab）](#7-配置变更)
8. [变更对比](#8-变更对比)
9. [同步历史](#9-同步历史)
10. [通知渠道（飞书）](#10-通知渠道)
11. [外部接口（Webhook / API Token）](#11-外部接口)
12. [用户与权限](#12-用户与权限)
13. [安全认证（MFA）](#13-安全认证)
14. [常见问题](#14-常见问题)

---

## 1. 登录与账号安全

- 默认账号：用户名 `admin`，密码 `admin123`。
- **首次登录请立即**：右上角头像菜单 → 修改密码；并到「安全认证」启用 MFA。
- 如果账号开启了 MFA，登录时会多一步：输入手机验证器里的 6 位动态码。

---

## 2. 整体概念

先理解三个对象，后面所有功能都围绕它们：

- **数据源**：一个 GitLab 仓库连接，或一个 K8s 集群连接。
- **同步任务**：把"某个 GitLab 源 + 路径"和"某个 K8s 集群 + 命名空间"绑在一起，定义同步方向和规则。
- **方向**：
  - 正向 = GitLab → K8s（把仓库里的 YAML 应用到集群）。
  - 反向 = K8s → GitLab（把集群里的资源回写到仓库）。

一个任务对应一个"环境"。比如 UAT 和 PROD 是两个不同的 GitLab 源/路径，就建两个任务。

---

## 3. 数据源

左侧菜单「数据源」。分两类，用页签切换。

### GitLab 数据源
填写：
- 名称（自定义，任务里通过名称引用）
- URL（GitLab 地址，如 `https://gitlab.example.com`）
- Token（GitLab Access Token，需要 api 读写权限）
- Project ID（项目数字 ID）
- Branch（分支，如 `main`）
- Path（仓库内的基础路径，如 `NTSP/UAT`）

填好点「测试连接」确认能连通，再保存。Token 落盘前会自动加密。

### K8s 集群
填写：
- 名称
- Kubeconfig 内容（粘贴或上传 kubeconfig 文件）
- 命名空间（默认 `default`，任务里可覆盖）

同样建议先「测试连接」。

> 连接 K8s 报 `context deadline exceeded`：说明服务端到 K8s API Server 网络不通或超时，检查网络/防火墙，确认容器内能访问到 K8s 的 6443 端口。

---

## 4. 同步任务

左侧菜单「同步任务」。点「新建任务」填写：

| 字段 | 说明 |
|------|------|
| 名称 | 任务名，也会写进 GitLab 的 commit message |
| 项目(project) | 可选，用于项目级权限批量授权 |
| 方向 | 正向 / 反向 |
| GitLab 源 | 选一个 GitLab 数据源 |
| K8s 集群 | 选一个 K8s 数据源 |
| 仓库路径 | 可覆盖数据源里的 Path |
| 命名空间 | 可填多个，逗号分隔 |
| 资源类型 | 选择要同步的资源种类（ConfigMap、Deployment、Service…）。不选则用默认集合 |
| 包含/排除过滤 | 按资源名称做正则过滤，见下 |
| 触发模式 | 手动 / 定时 / 自动 |
| 通知渠道 | 可选，绑定一个飞书渠道 |

### 名称过滤（正则）
- **包含过滤**：只同步名称匹配该正则的资源。例如填 `lion-.*` 只同步以 `lion-` 开头的。
- **排除过滤**：跳过名称匹配该正则的资源。
- 系统资源（如 `kube-root-ca.crt`、默认 ServiceAccount、云厂商插件等）会自动跳过，无需手动配置。

### 触发模式
- **手动**：在任务列表点「同步」按钮触发。
- **定时**：按设定的间隔周期性执行。
- **自动**：监听 K8s 变化事件，资源变动后自动反向同步（带 5 秒防抖，避免一次编辑产生多个 commit）。

### 暂停 / 启动
- 任务列表里可暂停或启动任务，控制定时/自动模式是否运行。

---

## 5. 正向同步：GitLab → K8s

正向同步**必须经过审核**，不会直接改集群。

1. 在任务列表找到正向任务，点「同步」（或「预览」）。
2. 系统读取 GitLab 里的 YAML，和集群现状逐项对比，弹出**同步预览**窗口：
   - 显示总资源数、有变更数、无变更数。
   - 每个有变更的资源展示 YAML diff（左：集群当前，右：GitLab 期望）。
3. 勾选你要应用的资源（默认全选有变更的）。
4. 点「应用变更」，系统才真正下发到 K8s。
5. 应用完成后弹出**同步结果窗口**：显示总计/已同步/无变更跳过/失败的统计，以及已同步清单、失败清单和错误详情（有失败时用告警样式）。

> 预览过程对 1000 个左右资源约需 30 秒，属正常（要拉 GitLab 文件 + 列集群资源做对比）。
>
> 同步失败时，结果窗口和历史里会显示该资源的具体错误信息，不会只报"网络错误"。

---

## 6. 反向同步：K8s → GitLab

把集群资源回写到 GitLab。

1. 在任务列表找到反向任务，点「同步」。
2. 系统列出集群资源，清理掉运行时字段（如 status、clusterIP、自动注入的字段等），生成干净、可在别的集群直接应用的 YAML。
3. **只把有实际变化的资源**写回 GitLab，单次同步合并成**一个 commit**，message 里带任务名和资源数量。
4. 若任务绑定了飞书渠道，且本次有内容更新，会推送一条飞书卡片通知。

> 重要：判断"有没有变化"用的是语义对比（忽略字段顺序、格式、K8s 默认值），和正向同步逻辑一致，所以不会因为格式差异误报一堆变更。

---

## 7. 配置变更

左侧菜单「配置变更」。这是给开发人员改 ConfigMap 用的：编辑内容 → 提交审核 → 批准后写入 GitLab。**它只改 GitLab，不会动 K8s**。要下发到集群，再用对应的正向同步任务。

### 提交变更（开发人员）
1. 进入「配置变更」→「提交变更」页签。
2. 选择**环境（GitLab 路径）** —— 下拉里按 GitLab 仓库+路径去重展示（如 `gitlab-uat - NTSP/UAT`），不显示同步方向，支持搜索。UAT 和 PROD 选各自的路径。
3. 选择要修改的 **ConfigMap**（系统自动列出该路径下已有的 configmap，支持搜索）。
4. 系统自动加载它在 GitLab 里的当前 YAML，**默认为只读**（灰底不可改）。
5. 点右上角「编辑」进入可编辑状态，改完点「保存修改」暂存（或「取消」放弃）。保存后回到只读态，下方显示**变更预览**（左：当前，右：修改后）。
6. 填写**变更说明**（必填）。
7. 点「提交审核」。状态变为「待审核」，此时还没写 GitLab。若编辑未保存，「提交审核」会被禁用并提示先保存。

> 提交时如果该 ConfigMap 已有其他待审核申请，会弹出确认框告知你有几条待审、申请人是谁，并提示并发冲突风险，需点「仍要提交」才创建。

### 审核（有编辑权限的人 / 管理员）
1. 进入「审核列表」页签。可按 **状态 / 环境(任务) / 申请人 / ConfigMap 名称** 四个条件组合筛选。
2. 点「查看」看完整 diff、申请人、说明。
3. 选择：
   - **批准并提交 GitLab**：系统先做版本校验（见下），通过后把新内容提交到对应环境的 GitLab，commit message 带申请人和审核人。
   - **驳回**：可填备注，状态变为已驳回，不写 GitLab。

### 多人并发编辑与冲突处理（乐观锁）
系统不做实时协同编辑（每人改自己的副本），而是通过乐观锁防止相互覆盖：
1. 用户加载文件时，系统记录当时 GitLab 该文件的版本（内容哈希）作为「基线」。
2. 审核人点「批准并提交」前，系统重新读取 GitLab 当前版本与申请基线比对：
   - **一致**：正常提交；
   - **不一致**（说明期间已被别的变更合入）：**拒绝提交**，该申请自动标记为「已失效(冲突)」，并弹窗提示审核人。
3. 冲突申请在「查看」时会展示两个 diff：①申请基线 vs GitLab 最新（别人合入的改动）②申请基线 vs 本申请修改，方便对照。
4. 处理方式：**不自动合并**，由申请人基于最新内容重新编辑提交（等于人工合并+重新审核，安全可控）。

> 审核列表里，同一文件有多条待审申请时会打上金色「同文件N条待审」标签，提醒审核人注意先后顺序。

### 行尾空白的显示
有些 ConfigMap 的值是一大段多行配置（如 `application.yml`），可能包含行尾空格。为避免看不见导致误解：
- 系统在所有 **diff 对比视图**（配置变更预览、审核详情、冲突对照、同步历史、变更对比）中，会把每行**行尾的空格显示为黄色 `·`、Tab 显示为 `→`**，鼠标悬停提示该行行尾的空白字符个数。
- 这只是**显示层的可视化标记，不会修改 GitLab 里的任何内容**，行尾空格原样保留，程序读取不受影响。
- 注意：配置变更的**编辑框本体**是普通文本框，行尾空格在编辑时不可见；请以下方的 diff 预览为准来核对行尾空白。

### 权限说明
- 提交变更：需要对该任务有「查看」权限。
- 审核（批准/驳回）：需要对该任务有「编辑」权限；管理员不受限。
- 普通用户在审核列表里能看到自己提交的，以及自己有查看权限的任务下的所有申请。
- 申请人可删除自己未批准的申请；已批准的记录仅管理员可删。

---

## 8. 变更对比

左侧菜单「变更对比」。用于查看 GitLab 仓库里 YAML 在一段时间或两次提交之间改了什么（只看 GitLab，不碰 K8s）。

1. 选择**环境（GitLab 路径）** —— 与配置变更一样，按 GitLab 仓库+路径去重展示、不显示方向、支持搜索。
2. 选对比方式：
   - **按时间段**：选起止时间，看这段时间内 GitLab 的变化。
   - **按 Commit**：从下拉里选两个提交（From 旧 / To 新）做对比。
3. 点「对比变更」，下方列出每个变更文件（新增/修改/删除）及其 diff。
4. **按资源类型筛选**：默认显示全部，对比出结果后可多选资源类型（如只看 deployments、configmaps），数量实时显示。
5. **导出**：把当前（筛选后的）对比结果导出成一个文本文件，便于存档或分享。

> 正向任务和反向任务在这里没有本质区别，对比的都是该任务关联的那个 GitLab 仓库路径。

---

## 9. 同步历史

左侧菜单「同步历史」。记录每次同步的结果。

- 支持按 ConfigMap 名称、命名空间、方向、时间范围筛选。
- 列表分页显示，点开某一行可展开看本次同步每个资源的详细 diff（按需懒加载）。
- 历史自动保留 **30 天**，过期记录会自动清理。

---

## 10. 通知渠道

在「数据源」页的「通知渠道」页签管理飞书机器人。

1. 新增渠道：填名称 + 飞书机器人 Webhook 地址。
2. 可添加多个渠道。
3. 在同步任务里选择要用哪个渠道。
4. 目前在**反向同步有内容更新时**推送飞书卡片，把更新内容发到群里。

---

## 11. 外部接口

让外部系统（如 CI/CD）触发同步，调用必须带认证。

### 方式一：任务级 Webhook Token
1. 在同步任务列表点对应任务的「Webhook」/「生成 Token」按钮。
2. 系统给出调用地址和 curl 示例：
   ```
   POST /api/v1/hooks/sync/{任务ID}?token=xxxx
   ```
3. 点「重新生成」会废弃旧 Token。

> 注意：外部 Webhook 触发的正向同步当前是**直接执行、不经审核界面**。如对生产环境有审核要求，请评估后再开放该任务的 webhook。

### 方式二：用户级 API Token
- 管理员可为某个账号生成 API Token，该账号按其被授予的任务权限调用接口。
- 适合"一个账号管一组任务"的场景。

> 完整 API 路由见项目里的 `API.md`。

---

## 12. 用户与权限

左侧菜单「用户与权限」（仅管理员可见）。

### 角色
- **管理员(admin)**：全部权限，可管理用户。
- **普通用户(user)**：默认无任何任务权限，需授权。

### 授权粒度
- **任务级权限**：对单个任务授予「查看 / 同步 / 编辑删除」三档（独立勾选）。
- **项目级权限**：对一个 project 下的所有任务批量授权。
- 解析顺序：**任务级优先，其次项目级**。任务级显式设置过就以它为准，否则回落到项目级。

### 三档权限含义
| 权限 | 能做什么 |
|------|----------|
| 查看 | 看到任务、看历史、提交配置变更申请、做变更对比 |
| 同步 | 触发该任务的同步 |
| 编辑 | 改任务配置、审核配置变更申请、删除 |

---

## 13. 安全认证（MFA）

左侧菜单「安全认证」。

- 启用 MFA：扫码（兼容 Google Authenticator、Microsoft Authenticator、1Password 等），输入一次动态码确认。
- 启用后每次登录都要输入动态码。
- 管理员可在「用户与权限」里远程为任意用户**开启/关闭/重置** MFA。

> MFA 总是验证失败：检查服务器和手机的系统时间是否同步（误差需在 30 秒内）。让管理员重置密钥后重新扫码即可。

---

## 14. 常见问题

**Q：同步报"网络连接错误"？**
看浏览器 F12 → Network 里那条请求的状态码和响应体；看服务端日志有没有 error；确认服务端容器内能访问到 K8s API 和 GitLab。

**Q：反向同步后再正向同步报 "field is immutable"？**
升级到最新版即可，cleaner 会剥离运行时字段。若 GitLab 里已有"脏"YAML，编辑掉相关字段后再同步。

**Q：正向同步预览很慢？**
1000 个左右资源约 30 秒属正常，主要耗时在拉 GitLab 文件和列集群资源做对比。系统已用并发优化（拉取/列举 10~20 并发，应用 10 并发）。

**Q：配置变更里选不到 ConfigMap？**
确认该 ConfigMap 在 GitLab 里的路径符合布局 `{基础路径}/{命名空间}/configmaps/{名称}.yaml`（和反向同步写入的布局一致）。若仓库里还没有该文件，需要先通过反向同步把它写到 GitLab。

**Q：环境晋级（UAT → PROD）怎么做？**
UAT 和 PROD 各建独立的 GitLab 路径 + 独立同步任务。通过「配置变更」改对应环境的 ConfigMap 并审核提交，再用该环境的正向同步任务下发到对应集群。

---

如需新增功能或调整流程，联系系统维护者。
